import { EntityLoadingState } from "@/features/entity/ui/components/entity-loading-state";

export const WorkflowsLoadingState = () => {
  return <EntityLoadingState message="Loading workflows..." />;
};
