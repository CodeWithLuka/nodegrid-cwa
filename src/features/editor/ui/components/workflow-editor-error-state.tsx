import { EntityErrorState } from "@/features/entity/ui/components/entity-error-state";

export const WorkflowEditorErrorState = () => {
  return <EntityErrorState message="Error loading editor" />;
};
